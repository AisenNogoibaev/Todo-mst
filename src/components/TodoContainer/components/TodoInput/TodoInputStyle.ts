import styled from "styled-components";

export const Input = styled.input`
  border-radius: 3px;
  border: 1px solid #638cff;
  margin-top: 1em;
  padding: 1em;
  font-size: 1.2em;

  :focus {
    border-color: #0345ff;
    outline: none;
  }
`;
